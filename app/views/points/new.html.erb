<%= form_for(@point_delta, :url => points_path) do |f| %>
  <%= f.number_field :amount %> Points to
  <%= select :point_delta, :to_user_id, User.all.collect {
    |u| [u.email,u.id] } %> for
  <%= f.text_field :reason %>
<% end %>